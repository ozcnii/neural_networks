# Подготовка к устному экзамену «Нейронные сети» (СГАУ, лето 2025/26)

Этот проект — подготовка студента к **устному экзамену**. Студент готовится **с нуля**: считай, что
читатель не знает почти ничего. Твоя задача — помочь составить понятные ответы-тезисы, которые
человек сможет переписать на листочек и пересказать вслух.

---

## ❶ Главные правила (НЕ НАРУШАТЬ)

1. **НИКОГДА НЕ ВЫДУМЫВАЙ.** Любой факт должен опираться на материалы из папки `materials/`.
2. **У каждого блока тезисов указывай источник и место**, чтобы студент мог перепроверить.
   Формат пометки: `[Тюгашев, «АРХИТЕКТУРА ТРАНСФОРМЕР», ~стр. 73]` или `[backprop-PDF, стр. 2]`
   или `[История GPT.doc]`. Если факт собран из нескольких мест — перечисли все.
3. **Если ответа нет в источниках** — сначала ЯВНО напиши:
   `⚠️ В предоставленных материалах этого нет.`
   Затем, отдельным блоком с заголовком `Вне источников (общеизвестное):`, можно дать
   общеизвестный ответ — но **обязательно с этой пометкой**, чтобы было видно, что это не из учебника.
   Не смешивай источниковое и внеисточниковое в одном абзаце.
4. **Не приукрашивай уверенность.** Если в источнике факт изложен расплывчато — так и скажи.

---

## ❷ Стиль ответов

- **Максимально просто, на пальцах.** Объясняй как другу, который слышит термин впервые.
- Сначала **короткое определение одной фразой**, потом раскрытие.
- Всегда давай **аналогию из жизни** или **числовой пример «на пальцах»**, где это уместно
  (в backprop-PDF отличный образец стиля: «бросаем мяч в корзину с закрытыми глазами»).
- Избегай математики без расшифровки. Если есть формула — рядом словами объясни, что она делает.
- Помни: экзамен **устный**. Ответ — это **тезисы для пересказа**, а не сочинение. Делай списки,
  короткие пункты, которые легко произнести.

### Когда нужна иллюстрация
Препод требует: *«если вопрос требует иллюстрации — надо нарисовать»*. Студент смотрит ответы в
**VS Code / Cursor**, поэтому схемы делаем **в виде SVG-файлов** (не ASCII — это «уродливо»):

1. Создавай `.svg` в папке `answers/img/` с именем `qNN-название.svg` (напр. `q05-neuron.svg`).
2. Встраивай в md картинкой: `![понятная подпись](img/qNN-название.svg)`.
3. **Обязательно проверяй рендер**: `rsvg-convert -w 760 answers/img/qNN-x.svg -o /tmp/p.png`, затем
   посмотри `/tmp/p.png` инструментом Read. Если криво — правь и перерисовывай.

**Технические правила SVG (чтобы не было кракозябр):**
- Подстрочные/надстрочные индексы — **только** через `<tspan font-size="11" dy="4">i</tspan>`
  (Unicode-подстрочники вроде x-en, i-нижнее рендерятся в мусор — НЕ использовать).
- Шрифт: `font-family="-apple-system, 'Segoe UI', Roboto, sans-serif"`, белый фон `<rect fill="#fff">`.
- Кириллица работает; следи, чтобы текст не вылезал за рамки блоков/кружков (проверяй рендером).
- Палитра-образец: входы/выход — синий `#2563eb`, сумматор/«бум» — янтарный `#f59e0b`, активация/
  «успех» — зелёный `#16a34a`, смещение/трансформер — фиолетовый `#8b5cf6`, «заморозка»/ошибка —
  красный `#dc2626`. Скруглённые прямоугольники `rx="10"`, стрелки через `<marker>`.

**Простые сравнения** оформляй обычной **markdown-таблицей** (рендерится чисто), а не SVG и не ASCII.
Готовые образцы стиля — уже сделанные `answers/img/q03..q10*.svg`; бери их за основу для единообразия.

### Когда нужен пример кода
Препод требует: *«если примера (фрагмент программы) — готовить пример»*. Давай **короткий
фрагмент на Python** (как в учебнике — Keras/PyTorch, или nanoGPT-фрагменты из GPT-docx).
Код должен быть минимальным и понятным, с комментариями по-русски.

---

## ❸ Источники в `materials/` — как читать каждый

| Файл | Что внутри | Как читать |
|---|---|---|
| `Нейронные сети - уч.пос. - Тюгашев.pdf` (180 стр.) | **ОСНОВНОЙ учебник.** Покрывает почти все вопросы | Текст извлекается: `pdftotext "..." /tmp/t.txt` либо инструментом Read |
| `Алгоритм_обратного_распространения_ошибок.pdf` (8 стр.) | Backprop **на пальцах**: аналогия «бросок мяча»; сеть как цепочка операций; полный числовой пример с 1 нейроном (x=2, w=0.5, b=0.1, sigmoid, MSE → forward ŷ=0.7503, L=0.0624 → backward по цепному правилу → обновление весов α=0.1 → проверка, что ошибка упала); 2-слойный пример; итоговый алгоритм по шагам; проблема скорости обучения α (большая/малая/норм, ASCII); аналогия «команда поваров». **«Затухания градиентов» тут НЕТ** | ⚠️ Это **картинки**, `pdftotext` НЕ работает. Читать только инструментом **Read по страницам** (`pages: "1-3"`) |
| `Для занятия 30042026 GPT DS.pdf` (10 стр.) | Устройство GPT целиком. Ч.I теория: ЯМ, авторегрессия, история (RNN/LSTM/GRU → Seq2Seq+Attention Bahdanau-2014 → Transformer Vaswani-2017 → GPT decoder-only Radford-2018); токенизация; эмбеддинги + позиционные; внимание Q/K/V, scaled dot-product, каузальная маска, multi-head; блок декодера, LayerNorm, **остаточные связи** («чтобы градиенты не затухать», «деление на √d_k чтобы не взрывались» — пригодится для Q18). Ч.II: **полный код MiniGPT на PyTorch** (Embeddings, CausalMultiHeadAttention, FeedForward, DecoderBlock, MiniGPT, generate_text) — готовый пример кода | ⚠️ Тоже **картинки** — читать инструментом **Read по страницам** |
| `Для занятия 30042026 GPT PerplP.docx` | Устройство GPT/трансформера простыми словами (версия Perplexity), механизм внимания на примере | `textutil -convert txt -stdout "..."` |
| `Устройство GPT (1).docx` | GPT «на пальцах», ASCII-схема архитектуры, фрагменты кода nanoGPT (wte, wpe, dropout, блоки) | `textutil -convert txt -stdout "..."` |
| `История GPT.doc` | История языковых моделей: t9 → BERT → GPT-3 → InstructGPT → ChatGPT → DeepSeek R1, RAG, агенты | `textutil -convert txt -stdout "..."` |

**Важно про страницы Тюгашева:** номера страниц ниже — это номера из **оглавления книги** (печатные),
они могут отличаться от номеров страниц в PDF на 1–2. Если не сошлось — **ищи по заголовку раздела**
(они в оглавлении ЗАГЛАВНЫМИ), а не по номеру.

### Оглавление учебника Тюгашева (разделы → печатная стр.)
- ВВЕДЕНИЕ — 3
- **1. ОСНОВЫ МАШИННОГО ОБУЧЕНИЯ** — 5; История МО — 8; Классические задачи МО — 10; Питон в ИИ — 15
- **2. ВВЕДЕНИЕ В ИНС** — 25; История ИНС — 25; Классы задач ИНС — 27; Биологический нейрон — 29;
  Структура формального нейрона — 30; Архитектуры нейросетей — 34; Теоремы Колмогорова–Арнольда
  и Хехт-Нильсена — 37; Обучение нейросети — 39 (с учителем — 40, без учителя — 44); Пример на Питоне — 46
- **3. ПРИМЕНЕНИЕ СОВРЕМЕННЫХ НЕЙРОСЕТЕЙ** — 57; Сверточные сети — 57; Рекуррентные сети — 67;
  Архитектура «трансформер» — 73; Обучение с подкреплением — 76; Тенденции развития БЯМ — 85;
  Библиотеки (Keras — 98, PyTorch — 109, мед.изображения — 115, квартиры — 121); Компьютерное зрение — 130;
  Нейросети для естественного языка — 134 (1D-CNN — 141, тональность текста — 143); AutoML — 145;
  Нейросотрудники — 154; Промпт-инжиниринг — 160
- **11. ФИЛОСОФСКИЕ/ЭТИЧЕСКИЕ ПРОБЛЕМЫ** — 166; Нерешённые вопросы ИНС — 166; О киборгах — 171

---

## ❹ Карта вопросов → где искать (СВЕРЯЙ ПЕРЕД ОТВЕТОМ)

Полный список вопросов — в `Вопросы_2026_Нейросети.docx` (20 вопросов; номер **13 пропущен**, после 12 сразу 14).

| № | Вопрос | Где искать |
|---|---|---|
| 1 | Понятие ИИ, подходы к определению, Нацстратегия РФ | Тюгашев: Введение / нач. гл.1. **Нацстратегия есть**: Указ Президента РФ №490, «до 2030 года» (искать «Национальная стратегия») |
| 2 | Подходы: логический(символический) / статистические МО / ИНС, сильные и слабые стороны | Тюгашев: гл.1 + разрозненно (символический подход — в разделе про объяснимость ИИ и «гибридизацию логического и нейросетевого»). ⚠️ **Цельного сравнения в одном месте НЕТ** — собирать по кускам, недостающее помечать как «вне источников» |
| 3 | История ИНС | Тюгашев: «ИСТОРИЯ ИСКУССТВЕННЫХ НЕЙРОННЫХ СЕТЕЙ» (~25). МакКаллок–Питтс 1943 и т.д. |
| 4 | Классические задачи МО, плюсы и проблемы | Тюгашев: «КЛАССИЧЕСКИЕ ЗАДАЧИ МАШИННОГО ОБУЧЕНИЯ» (~10) |
| 5 | Формальный нейрон, функции активации, их виды | Тюгашев: «СТРУКТУРА ФОРМАЛЬНОГО НЕЙРОНА» (~30); функции активации (ReLU, сигмоида и др.) там же |
| 6 | Основные архитектуры современных ИНС | Тюгашев: «АРХИТЕКТУРЫ НЕЙРОННЫХ СЕТЕЙ» (~34) |
| 7 | Полносвязная глубокая сеть прямого распространения | Тюгашев: «АРХИТЕКТУРЫ…» (~34) + «ТЕОРЕМЫ КОЛМОГОРОВА–АРНОЛЬДА И ХЕХТ-НИЛЬСЕНА» (~37) |
| 8 | Сети с обратными связями (рекуррентные), классификация, примеры | Тюгашев: «РЕКУРРЕНТНЫЕ НЕЙРОННЫЕ СЕТИ» (~67) |
| 9 | LSTM и GRU | Тюгашев: «РЕКУРРЕНТНЫЕ…» (~67) — есть LSTM и GRU |
| 10 | Сверточные сети: история, идеи, применение | Тюгашев: «СВЕРТОЧНЫЕ СЕТИ» (~57); + «Компьютерное зрение» (~130) |
| 11 | Нейросети для текста: задачи, архитектуры | Тюгашев: «НЕЙРОСЕТИ ДЛЯ АНАЛИЗА ЕСТЕСТВЕННОГО ЯЗЫКА» (~134), 1D-CNN (~141), тональность (~143) |
| 12 | Архитектура «трансформер» | Тюгашев: «АРХИТЕКТУРА ТРАНСФОРМЕР» (~73) + `GPT PerplP.docx` + `Устройство GPT (1).docx` + `GPT DS.pdf` (визуально; там же ASCII-схема блока и **готовый код MiniGPT** для примера) + `История GPT.doc` |
| 14 | Современные ЯМ, промпт-инжиниринг | Тюгашев: «ВВЕДЕНИЕ В ПРОМПТ-ИНЖИНИРИНГ» (~160) + `История GPT.doc` (few-shot, instruct, RAG) |
| 15 | Вайбкодинг и будущее ИТ — **ваше мнение** | ⚠️ **Слова «вайбкодинг» в материалах НЕТ.** Контекст для рассуждения: Тюгашев «Нейросотрудники» (~154), «Тенденции БЯМ» (~85), гл.11. Сам термин и мнение — блок «вне источников» |
| 16 | Методы обучения; обучение однослойной сети | Тюгашев: «ОБУЧЕНИЕ НЕЙРОННОЙ СЕТИ» (~39), с учителем (~40); + backprop-PDF (пример с одним нейроном) |
| 17 | Проблемы глубокого обучения; метод обратного распространения ошибки | Тюгашев: «ОБУЧЕНИЕ…» (~39–45) + **backprop-PDF целиком** (визуально, 8 стр., пошагово: forward → цепное правило → обновление весов → 2-слойный пример → проблема α) |
| 18 | Градиенты в глубоком обучении, проблемы, **затухание градиентов** | ✅ **Объяснение ЕСТЬ в учебнике**: Тюгашев, «РЕКУРРЕНТНЫЕ…» (~стр. 67–68) — прямо «проблема **взрывного** градиента (веса >1) / **затухающего** градиента (веса <1)… множественное умножение весов… числа <1 → экспоненциально убывает». + backprop-PDF (градиентный спуск, цепное правило = произведение производных, проблема α большой/малый) + GPT-DS.pdf («остаточные связи, чтобы **не затухать**», «√d_k чтобы **не взрывались**»). Связку «производная сигмоиды <0.25» и ReLU как лекарство помечать «вне источников» |
| 19 | Влияние качества данных на результат, примеры | Тюгашев: искать по тексту (переобучение, MNIST/NIST, качество данных) — ⚠️ нет отдельного раздела, собирать по тексту |
| 20 | Какие параметры подбирают при обучении/использовании ИНС | Тюгашев: «ОБУЧЕНИЕ…» (~39), разделы про библиотеки (learning rate, эпохи, batch, dropout). Собирать по тексту |

---

## ❺ Формат файлов с ответами

Ответы складывай в папку **`answers/`**, по **одному файлу на вопрос**: `answers/q01.md`, `answers/q05.md`, …
(вопрос 13 пропускаем — его нет).

Шаблон файла ответа:

```markdown
# Вопрос N. <текст вопроса>

## 🎯 Коротко для запоминания (прочитал → повторил → вспомнил)
Только факты-крючки, без воды: 4–7 предельно коротких строк (термин + суть в 3–6 словах),
по которым мгновенно вспоминается, о чём вопрос. Это шпаргалка для быстрого повтора перед
экзаменом, НЕ развёрнутый ответ. Ставится первым блоком, сразу под заголовком.

## В двух словах (для начала ответа)
<1–2 фразы — суть>

## Тезисы (для пересказа вслух)
- пункт … [источник, место]
- пункт … [источник, место]

## Аналогия / пример на пальцах
<простыми словами>

## Иллюстрация            ← только если вопрос её требует
<ASCII-схема>

## Пример кода            ← только если вопрос его требует
```python
# ...
```

## ⚠️ Чего нет в источниках / вне источников
<если применимо: что отсутствует в материалах и что добавлено как общеизвестное>
```

---

## ❻ Рабочий процесс при ответе на вопрос
1. Найди раздел(ы) по карте выше. Сначала читай **Тюгашева**, потом дополняй docx/PDF.
2. Для backprop-PDF и GPT-DS.pdf используй **Read с параметром `pages`** (это картинки!).
3. Собери тезисы простым языком, к каждому блоку — пометку источника с местом.
4. Реши, нужна ли иллюстрация и/или код (см. требования препода) — и сделай их.
5. Чего нет в источниках — пометь и вынеси в отдельный блок «вне источников».
6. Сохрани в `answers/qNN.md`.
