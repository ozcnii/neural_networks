# Промт для выполнения ЛР-6 (вариант Б, LSTM, прогноз временного ряда)

Выполни 6-ю лабораторную работу по дисциплине «Нейронные сети» в этом репозитории
(/Users/qq/Documents/ssau/6sem/neural_networks), в новую папку ./lab6.

## ЗАДАНИЕ
Файл задания: `tasks/lab6.md`. Делаем **ВАРИАНТ Б** («поинтереснее»):
- своя задача для обучения с применением LSTM-слоя;
- написать и обучить модель;
- ограничений на библиотеки нет.

Конкретная задача (зафиксирована): **ПРОГНОЗ ВРЕМЕННОГО РЯДА на LSTM**.
Цель — оригинально и аккуратно, «чтоб преподу понравилось».

**ВАЖНО:** чужого примера решения для ЛР-6 нет. Опирайся на СТИЛЬ И СТРУКТУРУ
моих уже сданных/готовых работ в этом же репозитории:
- `lab5/main.py`, `lab5/report.md` — последняя работа (RL, PyTorch), главный ориентир;
- `lab2/main.py`, `lab2/report.md` — ориентир по оформлению отчёта и протоколов;
- `completed/ЛР_*.docx` — как выглядят зачтённые работы (только для общего вида).

Чужой пример из задания (колаб «Предсказание отказов оборудования») — НЕ копировать,
у нас своя задача (прогноз ряда).

## РЕЖИМ РАБОТЫ (обязательно)
Действуй по **deep plan**: сначала анализ существующих `lab5`/`lab2`, затем задай мне
уточняющие вопросы (через выбор вариантов), затем покажи план с оценкой уверенности
(plan confidence / implementation confidence) и получи моё одобрение. КОД до одобрения
плана не пиши. Если по ходу реализации модель не сходится — честно итерируй (как в lab5:
несколько попыток тюнинга), показывай, что менял и почему.

## СТЕК И ОКРУЖЕНИЕ
- Python 3.9 (`/usr/bin/python3`), CPU. Установлены: numpy, matplotlib, pandas, torch, gymnasium, pandoc.
- Модель на PyTorch, обязательно с `nn.LSTM`.
- matplotlib только в режиме Agg (без GUI-окон), кэш — в `./lab6/.matplotlib_cache`
  (как в `lab5/main.py`: задать MPLCONFIGDIR/XDG_CACHE_HOME до импорта matplotlib).
- Всё детерминировано: фиксируй `SEED=42` для random/numpy/torch. Результат должен
  воспроизводиться при повторных запусках.

## ЗАДАЧА «ПРОГНОЗ ВРЕМЕННОГО РЯДА» (ориентир, детали уточни в плане)
- Сгенерируй СВОЙ синтетический ряд детерминированно (например: тренд + сезонность
  (одна-две гармоники) + шум; можно осмысленно назвать — энергопотребление/температура/нагрузка).
  Сохрани его в `lab6/data/*.csv`.
- Скользящим окном сформируй обучающие последовательности (вход — окно длины L,
  цель — следующее значение, или прогноз на несколько шагов).
- Раздели на train/test по времени (без перемешивания — это временной ряд!),
  нормируй по статистикам train (без утечки из test).
- Модель: `nn.LSTM` (+ линейный голова), функция потерь MSE/SmoothL1, оптимизатор Adam.
- Обучи, логируй loss по эпохам. Если обучение нестабильно — сохраняй лучший чекпойнт
  по валидации/test (early stopping), как в lab5.
- Метрики на test: MAE и RMSE (в исходных единицах ряда). Сделай содержательные выводы.

## СТРУКТУРА ./lab6 (по образцу lab5)
- `main.py` — самодостаточный: генерация данных, подготовка окон, модель LSTM,
  обучение, оценка, построение всех графиков и протокольных PNG.
- `requirements.txt` — torch, numpy, matplotlib (что реально используется).
- `data/` — сгенерированный CSV ряда.
- `artifacts/` — training_log.csv, графики (история loss, прогноз vs факт), веса модели (.pt).
- `protocol/` — набор PNG-«скриншотов» консоли + графики, как в `lab5/protocol`:
  - описание задачи/датасета (текстовый PNG),
  - структура модели LSTM + число параметров и гиперпараметры (текстовый PNG),
  - начало обучения и конец обучения (текстовые PNG с логом эпох),
  - график кривой обучения (loss train/val по эпохам),
  - график «прогноз vs фактический ряд» на тесте (главная иллюстрация),
  - по желанию: график остатков/ошибок или прогноз на несколько шагов вперёд.

  Текстовые PNG рендерь функцией по образцу `save_text_protocol` из `lab5/main.py`
  (matplotlib, моноширинный шрифт, axis off).
- `report.md` — отчёт по шаблону вуза (см. ниже). **DOCX НЕ СОБИРАТЬ.**

## ШАБЛОН report.md (точно как lab5/report.md и lab2/report.md)
- Титульный блок: тот же вуз (САМАРСКИЙ УНИВЕРСИТЕТ, ИИК, кафедра программных систем),
  ОТЧЕТ по лабораторной работе №6, дисциплина «Нейронные сети», тема в кавычках
  (например «Прогнозирование временного ряда с помощью рекуррентной нейронной сети (LSTM)»),
  Студент: Фокин Евгений Андреевич, Группа: 6303-020302D, Проверил: профессор Тюгашев А.А.,
  Дата, «Самара 2026». Используй точное форматирование из `lab5/report.md` (заголовки, `<br>`, прочерк даты).
- Содержание (ссылки-якоря).
- Постановка задачи: цель, что такое LSTM/RNN кратко, описание своей задачи и датасета, список подзадач.
- Исходный текст программы: ссылка на main.py + requirements.txt; перечисли, что реализовано;
  приведи 1–2 ключевых фрагмента кода (создание модели/обучение/формирование окон).
- Протокол исполнения: каждый рисунок с подписью «Рисунок N - …» и вставкой
  `![alt](protocol/имя.png)`, пути относительные.
- Заключение: с КОНКРЕТНЫМИ числами (итоговые MAE/RMSE на test, число эпох, размер модели),
  выводы о сходимости и качестве прогноза.
- Список использованных источников: учеб. пособие Тюгашева; документация PyTorch;
  при желании классические ссылки (Hochreiter & Schmidhuber, LSTM, 1997) — оформи как в `lab5/report.md`.

## ПРОВЕРКА (verification)
- Запусти `python3 lab6/main.py` целиком — должен отработать без ошибок и предупреждений
  (проверь и с `-W error::RuntimeWarning`).
- Убедись, что все протокольные PNG и артефакты сгенерированы и осмысленны
  (открой ключевые изображения и проверь: кривая loss убывает, прогноз близок к факту).
- Числа в report.md должны совпадать с реальным выводом программы.
- Прогон детерминирован (повтори запуск — те же метрики).

## ЧИСТОТА РЕПОЗИТОРИЯ
- Добавь `./lab6/.matplotlib_cache` в `.gitignore` при необходимости (в репо уже есть правило
  для `.matplotlib_cache` — проверь). Кэш matplotlib не коммить.
- Никаких console.log/TODO/закомментированного мусора. Без хардкода «красивых» цифр —
  все метрики берутся из реального прогона.
- Коммит/деплой НЕ делай, если я об этом не попрошу.

В конце предложи провести deep review (как в нашем рабочем процессе).
